# 🗂️ Automatic File Organizer

A simple yet powerful Python script that **automatically organizes files** in your folder (like the Downloads folder) into subfolders based on their file extensions.  

Example:  
All `.jpg` files go into a `jpg` folder, `.pdf` files go into a `pdf` folder, etc.

---

## 🚀 Features
- 🧹 Automatically sorts files by type (extension)
- 📁 Creates new folders if they don’t exist
- 🔄 Moves files safely into categorized folders
- ⚙️ Simple and fast — perfect for cleaning up messy folders

---

## 🧰 Requirements

Make sure you have **Python 3.x** installed.  
No external libraries are required — uses only the Python standard library (`os`, `shutil`).
